@model Task1_Homework.Business.Ticket

@{
    ViewData["Title"] = "Edit Ticket";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="loginpagetext buytext" >Edit</h1>

<div class="row blockoedit">
    <div class="col-md-4">
        <form asp-action="Edit" class="buytextmini maformnmm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.HiddenFor(m => m.Id)
            <div class="form-group">
                <label class="control-label"><strong>Event:</strong></label>
                @Html.DisplayFor(model => model.Event.Name)
            </div>
            <div class="form-group">
                <label class="control-label"><strong>Seller:</strong></label>
                @Html.DisplayFor(model => model.Seller.UserName)
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" asp-action="Edit" class="btnsend" /> |
                <a asp-action="Index" class="btncancel">Cancel</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
